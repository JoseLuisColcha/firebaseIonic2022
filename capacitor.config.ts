import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firebase2022A',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
